{
  "name": "notes-vault-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development bun run --watch src/main.tsx",
    "build": "NODE_ENV=production bun build ./src/main.tsx --outdir ./dist --minify",
    "serve": "bun run --bun ./dist/main.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.0.0"
  }
}